version: '3.8'

services:
  bot:
    build: .
    environment:
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - DATABASE_URL=${DATABASE_URL:-not_set}
      - CUSTOM_VAR=${CUSTOM_VAR:-test_value}
      - NODE_ENV=production
    restart: unless-stopped
