# 🚀 Как задеплоить этот проект через GHOST SHELL

## 📋 Информация о проекте

**GitHub Repository:** https://github.com/lexxxx89/test_repo.git

**Что внутри:**
- Nginx веб-сервер (порт 80)
- Node.js Express API (порт 3000)
- Готовый docker-compose.yml

---

## 🎯 Шаги для деплоя

### 1. Открой Telegram мини-апп GHOST SHELL
Перейди в бота и открой мини-приложение

### 2. Создай новый проект
- Нажми "Create New Project" или "+"
- Введи название проекта (например: `test-app`)

### 3. Укажи данные репозитория
**Repository URL:**
```
https://github.com/lexxxx89/test_repo.git
```

**Branch:** `main`

**GitHub Token (если требуется):**
```
YOUR_GITHUB_TOKEN_HERE
```

### 4. Выбери облачный провайдер
- **Timeweb** (уже настроен в админке)
- или **Hetzner**

### 5. Дождись деплоя
- Создание сервера (~2-3 минуты)
- Pull образов и запуск
- Получишь IP адрес

### 6. Проверь работу

После деплоя откроешь:
- `http://<SERVER_IP>` - красивая страница
- `http://<SERVER_IP>:3000` - Node.js API
- `http://<SERVER_IP>:3000/health` - health check

---

## ✅ Проверка в админке

После создания проекта можешь посмотреть в админке:
- **Projects** → найдешь свой проект
- **Servers** → увидишь созданный сервер
- **Activity** → логи деплоя

---

## 🔧 Что тестировать

1. ✅ Создание проекта из GitHub репы
2. ✅ Автоматический деплой
3. ✅ Docker Compose запуск
4. ✅ Несколько сервисов (nginx + node)
5. ✅ Health checks
6. ✅ Доступ по IP

---

## 📝 Настройки в docker-compose.yml

```yaml
services:
  web:
    image: nginx:alpine
    ports:
      - "80:80"    # Главная страница
  
  app:
    image: node:20-alpine
    ports:
      - "3000:3000"  # API сервер
```

---

## 🐛 Если что-то не работает

1. Проверь логи в мини-аппе
2. Посмотри в админке (Projects → детали проекта)
3. Проверь что Timeweb API token сохранен в Settings
4. Проверь баланс в Timeweb (должны быть деньги для создания сервера)

---

**Готово к тестированию! Попробуй создать проект в мини-аппе! 🚀**
