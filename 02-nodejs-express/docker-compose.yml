version: '3.8'

services:
  api:
    build: .
    ports:
      - "80:80"
    environment:
      - NODE_ENV=production
      - PORT=80
      - CUSTOM_VAR=${CUSTOM_VAR:-default_value}
    restart: unless-stopped
